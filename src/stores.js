import { writable } from "svelte/store";

export const ZIPCODE = writable(undefined);

export const WEATHER = writable(undefined);

export const UNITS = writable(undefined);

export const FIVEDAY = writable(undefined);
